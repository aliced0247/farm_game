# 🛒 かいものリスト — 家族で共有できる買い物リスト

家族みんなのスマホで同じ買い物リストが見られるアプリです。

- だれかが「牛乳」を追加 → **全員のスマホに自動で出る**
- お店でチェック → **全員のリストからチェックされる**（買いかぶり防止）
- **マージとか同期ボタンとか一切なし**。ぜんぶ自動。
- 電波がなくても使える（つながったら勝手に同期）
- ホーム画面に追加すればアプリみたいに使える

**URL（Pages公開後）**: https://aliced0247.github.io/farm_game/kaimono.html

---

## 使いはじめかた

### まずは1人で（設定なしですぐ使える）

URLを開くだけ。追加・チェック・削除、ぜんぶ動きます。データはそのスマホに保存されます。

### 家族と共有する（最初に代表1人だけ10分の作業）

共有には「家族共通の保存場所」が1つ必要です。Google の無料サービス
**Firebase** を使います（買い物リスト程度の量なら**ずっと無料**です）。

#### 代表の人の手順

1. [console.firebase.google.com](https://console.firebase.google.com) を開いて、Googleアカウントでログイン
2. **「プロジェクトを追加」** → 名前は何でもOK（例: `kaimono`）→ Google Analytics は**オフ**でOK → 作成
3. 左メニュー **「構築」→「Realtime Database」** → **「データベースを作成」**
   - 場所: **Singapore (asia-southeast1)** がおすすめ
   - モード: **「ロックモード」** を選んで有効化
     （⚠️「テストモード」は30日で使えなくなるので選ばない）
4. **「ルール」タブ** を開いて、中身をぜんぶ次のように書きかえて **「公開」** ボタンを押す:

   ```json
   {
     "rules": {
       "rooms": {
         "$room": {
           ".read": true,
           ".write": true
         }
       }
     }
   }
   ```

5. **「データ」タブ** の上に表示されている URL（`https://〇〇-default-rtdb.〇〇.firebasedatabase.app/` みたいなやつ）をコピー
6. アプリを開いて **⚙️（右上）→「あたらしく共有をはじめる」** に URL を貼り付けて **「共有をはじめる」**
7. できあがったら **「📨 招待リンクを家族に送る」** で LINE などで家族に送る

#### 家族の手順（かんたん）

送られてきた**リンクを開いて、名前を入れるだけ**。おわり。

---

## スマホのホーム画面に追加（アプリ化）

- **iPhone (Safari)**: 共有ボタン（□↑）→「ホーム画面に追加」
- **Android (Chrome)**: メニュー（⋮）→「ホーム画面に追加」/「アプリをインストール」

> ⚠️ **iPhoneの注意**: ホーム画面に追加したアプリは Safari と別のデータになるので、
> 共有設定が消えたように見えることがあります。その場合はアプリを開いて
> **⚙️ → 「招待リンクで参加する」** に、LINEで送られたリンクを貼り付ければ復活します。

---

## しくみ（技術メモ）

- `kaimono.html` 単体で動く（ビルド不要・ライブラリ不要、このリポジトリの他ページと同じ方式）
- 同期は Firebase Realtime Database の **REST API + SSE ストリーミング**（SDK不使用）
- データは `rooms/<ランダム20文字のID>/items/<品物ID>` に保存
  - 部屋IDは推測できないランダム文字列（招待リンクを知っている人だけがアクセスできる）
- **衝突解決**: 品物ごとに `t`（最終更新時刻）が新しい方が勝つ Last-Write-Wins。
  書き込みは品物単位・フィールド単位の PATCH なので、家族が同時に別の品物を
  さわっても衝突自体がほぼ起きない → **手動マージ不要**
- **オフライン**: 変更は localStorage のキューに貯めて、オンライン復帰・画面復帰時に自動送信
- PWA対応: `kaimono-sw.js`（オフラインでも開ける）+ `kaimono.webmanifest` + `icons/`
- テスト: モックRTDBサーバー + Playwright で「2台同期・オフライン復帰・同時編集マージ・招待リンク参加」を検証済み

## ファイル一覧

| ファイル | 役割 |
|---|---|
| `kaimono.html` | アプリ本体（これだけで動く） |
| `kaimono-sw.js` | Service Worker（オフライン用キャッシュ） |
| `kaimono.webmanifest` | ホーム画面追加用の設定 |
| `icons/kaimono-192.png` / `kaimono-512.png` | アプリアイコン |
