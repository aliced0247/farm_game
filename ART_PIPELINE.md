# 作画SVG パイプライン（手描き／ツール書き出しSVGの差し込み方）

このゲームのアイテムの見た目は、デフォルトでは **コードによるプロシージャル描画**
（`renderTops` などが属性カラー＋レア度で形を生成）です。
ここに **「ツールで描いたSVG」を1アイテム単位で差し込んで上書き** できます。
手打ちパスを卒業して、Figma / Illustrator / Inkscape / Affinity で
「見ながら描いて → SVG書き出し → 貼る」だけで質を上げるための仕組みです。

---

## 1. キャンバスと座標系

キャラ全体は **`viewBox="0 0 280 330"`**（幅280・高さ330）の座標で描かれます。
作画SVGも **この座標系に合わせて** 描いてください（書き出し後に座標を合わせる）。

各スロットのだいたいの描画領域：

| スロット   | x 範囲   | y 範囲   | メモ |
|-----------|---------|---------|------|
| `hair`    | 50–230  | 14–262  | 顔の後ろ(back)と前(front)に分かれる |
| `tops`    | 62–218  | 184–250 | **袖も自分で描く**（素体の腕は出ない） |
| `bottoms` | 70–210  | 224–292 | スカート/パンツ |
| `shoes`   | 108–174 | 300–330 | 左右の足 |
| `acc`     | 任意     | 任意     | 顔の中心 cx140 / cy108 を基準に |
| `bg`      | 0–280   | 0–330   | 全面（部屋の手前・キャラの後ろ） |

参考：頭は `cx=140 cy=108 rx=70 ry=74` の楕円。首は y≈168–186。脚は y≈286–320。

---

## 2. 描くときのルール（重要）

- **塗りつぶしは自己完結の色で**（タイトルバーの属性カラーに依存しない）。
- **`tops` を差し替えると腕が消える** → 袖を含めて描く。
- 肌の色に動的に合わせたい部分は、`art` を **関数**で渡して
  `fill="url(#skin_<id>)"` を使う（下記4参照）。固定色でよければ文字列でOK。
- レア度の演出（きらめき・宝石）はコード側ヘルパーを使い回せる：
  - `gemSVG(x, y, r, color)` … 宝石
  - `sparkSVG(x, y, size, color)` … 1個のきらめき
  - `ssrAura([[x,y,size,color], ...])` … きらめきをまとめて
- 書き出し時は **不要なメタ情報を削る**（`width/height` 属性、`<?xml>`、エディタ固有タグ）。
  中身の `<path>`/`<g>` だけを貼るのが理想。

---

## 3. 差し込み方（コード）

`fantasy_market.html` 内、`ITEM_BY_ID` 定義の直後に `art()` ヘルパーがあります。

```js
// frame: そのアイテムの見た目（下表）／thumb: 一覧アイコン用(任意, viewBox 0 0 24 24)
art("アイテム名", frame, thumb);
```

`frame` の型はスロットで異なります：

| スロット | frame の型 |
|---------|-----------|
| `tops` / `bottoms` / `shoes` / `bg` | SVG文字列（例 `` `<g id="top">…</g>` `` ） |
| `hair` | `{ back: "…", front: "…" }` |
| `acc`  | `{ body: "…", head: "…" }` |

例（トップス）：

```js
art("いちごブラウス",
  `<g id="top"> …描いたSVGの中身… </g>`,
  `<path d="…24x24用の小さいアイコン…"/>`   // thumb は省略可
);
```

実装済みの見本：**「くろレースドレス」**（gothic SSR）。
コード内の `art("くろレースドレス", …)` ブロックがそのまま雛形になります。

---

## 4. 肌色を動的に合わせたい場合（応用）

`art` は文字列の代わりに **`function(id)`** も渡せます。`id` を使うと
そのレンダリングの肌グラデ `skin_<id>` を参照できます。

```js
art("ノースリーブ", (id)=>`<g id="top">
  <path d="…肌が見える腕…" fill="url(#skin_${id})" stroke="#8B5E6B" stroke-width="2"/>
  <path d="…服本体…" fill="#7fa6cc"/>
</g>`);
```

---

## 5. 制作フローの推奨

1. ベクターツールで **280×330 のフレーム**を用意（ガイドに頭・首・腰・足の基準線）。
2. 1スロットずつ描く。`tops` は袖込み。
3. SVG書き出し → 中身の `<path>/<g>` を取り出す。
4. `art("名前", …)` で貼る → ブラウザで着せ替えして位置を微調整。
5. パレット・線幅・陰影を全アイテムで統一（ここが“質”の正体）。

> ヒント：まず代表アイテム（各スロット1点＝6点）だけ作画SVG化して世界観を固め、
> 残りは徐々に差し替えるのが挫折しにくいです。未差し替えのアイテムは
> 従来のプロシージャル描画のまま動くので、混在OK。

---

## 6. キャラ工房（atelier.html）

スマホだけで作画フローを回すための GUI を `atelier.html` に用意した（Pages で公開）。
本体 `fantasy_market.html` の描画エンジンを**実行時に読み込んで**使うので、
本体を直すと工房にも自動反映される（描画ロジックの二重管理なし）。

- **顔スタジオ（Phase 1・実装済み）**：顔の輪郭（はば／たかさ／あご／ほっぺ）・目・眉・口・
  目のサイズ/間隔・パーツ位置・肌/髪色を**スライダーとチップで即時調整**。
  値は `S.face` に保存され、**ゲーム本体にも反映**される。
  - 追加した顔パラメータ：`faceW` `faceH` `chin` `cheek`（`defaultFace()` に既定値。既存セーブは自動補完）。
- **作画スタジオ（Phase 2・予定）**：SVGを貼って人形上で位置合わせ→`art()` コードを生成する GUI。
  この §1〜§5 の手順をツールなしで完結できるようにする。

> 開き方：Pages の `…/farm_game/atelier.html`。`file://` 直開きは CORS で不可。
